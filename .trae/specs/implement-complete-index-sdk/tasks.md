# Tasks
- [x] 任务 1：梳理官方 `meilisearch-js` 与当前桥接实现之间的最小对齐面，明确第一阶段必须支持的 `Client`、`Index`、任务与设置接口。
  - [x] 子任务 1.1：盘点当前 `packages/api/src/index.ts` 的公开方法、返回值与错误形状
  - [x] 子任务 1.2：定义第一阶段必须支持的官方风格方法与可接受差异边界
  - [x] 子任务 1.3：更新 Rust/TS 之间需要共享的任务、搜索、设置与错误数据结构

- [x] 任务 2：在 `packages/core` 中接入真实任务执行路径，替换当前伪任务与占位式写入逻辑。
  - [x] 子任务 2.1：确定并接入 vendored Meilisearch 中适合桥接的任务/调度执行入口
  - [x] 子任务 2.2：实现文档写入任务，保证任务完成后数据可被搜索
  - [x] 子任务 2.3：实现基础设置更新任务，并将结果映射为稳定的 N-API 导出结构
  - [x] 子任务 2.4：实现任务查询/等待接口，供 TypeScript SDK 消费

- [x] 任务 3：在 `packages/core` 中实现真实 `search` 执行路径与结果映射。
  - [x] 子任务 3.1：实现最小可用的查询执行逻辑
  - [x] 子任务 3.2：将底层搜索结果映射为稳定的 JS/TS 结果对象
  - [x] 子任务 3.3：补齐空查询、无结果与常见失败场景的错误映射

- [x] 任务 4：重构 `packages/api` 的 `Client` / `Index` 对外行为，使其尽量贴近官方 `meilisearch-js` 常用使用体验。
  - [x] 子任务 4.1：调整方法签名、任务返回值与等待能力
  - [x] 子任务 4.2：统一错误标准化逻辑与类型声明
  - [x] 子任务 4.3：补齐必要的 `.d.ts` / 包入口，避免未构建场景下类型检查失败

- [x] 任务 5：补充自动化验证，确保第一阶段 Index 核心能力可回归验证。
  - [x] 子任务 5.1：新增或更新 `packages/api` 集成测试，覆盖索引创建、加文档、等待任务、搜索、更新设置
  - [x] 子任务 5.2：补充针对典型错误输入的测试
  - [x] 子任务 5.3：验证相关 CI 工作流能在所需构建顺序下通过

# Task Dependencies
- 任务 2 依赖 任务 1
- 任务 3 依赖 任务 2
- 任务 4 依赖 任务 2、任务 3
- 任务 5 依赖 任务 2、任务 3、任务 4
